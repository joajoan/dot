# Python.
alias python=python3
alias pip=pip3
alias fd=fdfind

# Databricks
alias db=databricks
alias dab="databricks bundle"
alias dbfs="databricks fs"
alias dbsql=dbsqlcli

# System.
alias sudo="sudo env PATH=$PATH"
